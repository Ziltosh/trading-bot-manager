import { Step } from "react-joyride";

export interface Tours {
    root: Step[];
    robots: Step[];
    dashboard: Step[];
    comptes: Step[];
    optimisations: Step[];
    optimisationView: Step[];
}
